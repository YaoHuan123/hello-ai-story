# Android：frontend/.env.android.example
# iOS：frontend/.env.ios.example
# 生产示例：
# VITE_API_BASE_URL=https://api.yourdomain.com
#
# 界面语言（可选）：VITE_LOCALE=zh | en
# Web 以 backend APP_LOCALE + /api/health 为准；此项仅 health 不可用时的兜底。原生包见 docs/i18n.md
