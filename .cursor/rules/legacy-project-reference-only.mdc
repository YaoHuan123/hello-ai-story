---
description: 老项目（E:\hello story）只能参考需求与提示词，禁止复用其代码
alwaysApply: true
---

# 老项目仅作需求/提示词参考，禁止复用代码

老项目 `E:\hello story` 代码实现混乱、存在问题，当前项目 `E:\hello story2` 是为重写而新建。

## 允许

- 参考老项目的**需求 / 业务逻辑意图**
- 参考老项目的**提示词文案**（如 `prompts/**/*.md`）作为起点

## 禁止

- 复制、移植老项目的 `.ts` / `.js` 等**源码**
- 因为「老项目这么写」而引入大量 service / 类型 / 工具文件
- 照搬老项目的目录结构与抽象分层
- **直接照搬老项目提示词**：老提示词往往覆盖了当前不需要的分支/字段/格式

## 提示词处理

老提示词只作**参考起点**，必须按当前功能**精简后再用**：

- 只保留当前功能真正用到的输入字段、判定原则与输出格式
- 删掉无关分支（如只做 Tier1 就删掉 Tier2/Tier3、generatedPicks、tier3Only 等）
- 输出形态尽量扁平、与本项目代码校验对齐
- 保留影响质量的关键约束（常识判断、去重、字段字面一致等），不要为精简而误删

## 做法

- 按当前项目需求**重新实现**，保持模块最小、聚焦单一职责
- 一个功能优先用一个小模块（几个文件）解决，不要一上来铺开十几个 service
- 如需老项目逻辑，先理解需求，再用干净代码表达，而非粘贴

<example>
需求：LLM 推荐话题（只做 Tier1）
- ✅ 新建单模块 `backend/src/topic/`：加载配置模板话题 + 最小 LLM 调用
- ✅ 把老的 gating 提示词精简成自包含 Tier1 版（扁平输出 `{ pick: {...} }`），删掉 Tier2/3 段
- ❌ 把老项目的 interviewGating / templateCatalogV2 / openaiCompat 等十几个文件搬过来
- ❌ 原样拼接老的 `sub-category-gating.md` + `sub-category-gating-tiered.md`
</example>
