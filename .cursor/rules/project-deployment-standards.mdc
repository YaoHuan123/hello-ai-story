---
description: 全项目通用 — hellotita.top 路径、服务器 PM2 部署、iOS App Store 上架基线
alwaysApply: true
---

# 项目部署与上架基线（全项目通用）

适用于 hellotita.top 下所有产品。`{slug}` = 仓库名（kebab-case，如 `hello-story`）。

## 1. 域名与路径（仅 hellotita.top）

- **不买额外域名**；所有项目共用 `https://hellotita.top`，用**路径**区分，不用子域名。
- 每个项目固定以下公开 URL（静态页或反代，须 HTTPS）：

| 用途 | URL |
|------|-----|
| 隐私政策 | `https://hellotita.top/{slug}/privacy` |
| 用户协议 | `https://hellotita.top/{slug}/terms` |
| Support | `https://hellotita.top/{slug}/support` |
| API（若有后端） | `https://hellotita.top/{slug}/api` → 反代本机端口 |

- **禁止**两个 App 共用同一份未区分产品的隐私政策；页面标题与正文须写明**产品名**。
- App / 构建配置里的 API 基址写完整 HTTPS 路径，例如 `VITE_API_BASE_URL=https://hellotita.top/{slug}/api`。
- **法律/Support 页放在本项目仓库内**（推荐 `backend/public/legal/*.html`），由本项目 API 进程提供（如 `/privacy`）；Nginx 将 `hellotita.top/{slug}/privacy` 反代到该项目端口。**禁止**把各 App 的法律页挂到其它项目目录或主站静态根目录。

## 2. 服务器环境（Ubuntu 22 + PM2）

- 生产后端统一运行在 **Ubuntu 22.04**。
- 进程管理只用 **PM2**（与 systemd 二选一；全项目规范以 PM2 为准，勿同一 worker 双启）。
- 服务器需预装：**Node.js LTS**（建议 22.x）、**npm**、**pm2**（`npm i -g pm2`）、**ffmpeg**（若项目有视频管线）。
- 部署用户示例：`admin`；代码目录见下节。

### 目录

- 服务器路径固定：`/home/admin/apps/{仓库名称}`（与 git 仓库名一致）。
- `.env`、数据目录、`node_modules` 仅存在于服务器该路径，勿提交密钥。

### 一键启动脚本（必须）

每个有 Node 后端的项目必须在 **`backend/deploy/start.sh`**（或仓库根 `deploy/start.sh`，文档写清）提供脚本，在服务器上**一条命令**完成：

1. 检查依赖命令（`node` / `npm` / `pm2`）与 `.env`
2. `npm ci`
3. `npm run build`
4. 首次 `pm2 start ecosystem.config.cjs`，已存在则 `pm2 restart ecosystem.config.cjs`
5. 可选 `pm2 save`；打印 `pm2 list`

约定：

- 脚本 `#!/usr/bin/env bash` + `set -euo pipefail`
- 可从 **backend 目录**或仓库根 `npm run deploy:backend` 调用
- README 中写明 Ubuntu 22 首次部署与更新均执行该脚本

Hello Story 示例：

```bash
cd /home/admin/apps/hello-story/backend
chmod +x deploy/start.sh   # 首次
./deploy/start.sh
# 或：cd /home/admin/apps/hello-story && npm run deploy:backend
```

### ecosystem.config

- 每个**有 Node 后端**的项目必须提供 PM2 配置：`ecosystem.config.cjs`（或 `.ts`，二选一，推荐 `.cjs` 免编译）。
- 放在**后端包根目录**（如 `backend/ecosystem.config.cjs`）；多进程（API + worker）写在同一文件的 `apps` 数组里。
- 约定：
  - `name`: `{slug}-api`、`{slug}-worker`（按实际需要增减，名称全局唯一）
  - `cwd`: 使用 `__dirname` 或 `/home/admin/apps/{仓库名称}/backend`
  - `script`: 指向 `dist/...`（先 `npm run build` 再 `pm2 start`）
  - `instances: 1` + `exec_mode: "fork"`（除非项目文档明确需要 cluster）
  - `env`: 至少 `NODE_ENV: "production"`；敏感项走服务器 `.env`，PM2 里用 `env_file` 或部署脚本注入
- 根 `package.json` 提供快捷脚本：`pm2:start` / `pm2:stop` / `pm2:logs`（可选）。
- 仓库内附简短部署说明（如 `backend/deploy/README.md`）：构建命令、PM2 启停、Nginx 反代 `{slug}/api` 与 `{slug}/privacy|terms|support` 示例。

### Nginx 反代示例（示意）

```nginx
location /hello-story/api/ {
  proxy_pass http://127.0.0.1:3001/api/;
  proxy_set_header Host $host;
}
location ~ ^/hello-story/(privacy|terms|support)$ {
  proxy_pass http://127.0.0.1:3001/$1;
}
```

## 3. iOS App Store 上架基线

有 **Capacitor / 原生 iOS 包** 的项目，在规划功能时默认包含下列项（无 iOS 则跳过本节）。

### 法务与 Connect（仓库外但必须交付）

- [ ] **Privacy Policy URL** → `https://hellotita.top/{slug}/privacy`
- [ ] **Terms / EULA** → `https://hellotita.top/{slug}/terms`
- [ ] **Support URL** → `https://hellotita.top/{slug}/support` 或公开邮箱
- [ ] App Store Connect：描述、截图、**Privacy Nutrition Labels**、年龄分级、出口合规问卷
- [ ] 若用 AI 生成内容：审核备注与 App 描述中说明；产品内加**准确性免责声明**

### App 内（代码）

- [ ] 登录页 +「我的/设置」：**Privacy / Terms / Support** 可点击链接（`Linking.openURL` 或 in-app WebView）
- [ ] **Sign in with Apple**（若 iOS 有第三方/自有账号登录则必选；仅 Apple 亦可）
- [ ] **账号注销**（Apple 用户需再验证身份），入口在账户页
- [ ] 生产构建：`APPLE_AUTH_DEV_MOCK=0`；`Info.plist` 去掉仅用于开发的 `NSAllowsLocalNetworking` 等例外
- [ ] 原生包语言：`VITE_LOCALE=en`（美区）或按市场；后端 `APP_LOCALE` 与目标市场一致（采访/LLM 语种跟随服务端）
- [ ] 生产 API：`VITE_API_BASE_URL=https://hellotita.top/{slug}/api`（HTTPS，非局域网 IP）

### 工程与签名

- [ ] `ios/` 工程、Bundle ID、Sign in with Apple entitlement
- [ ] **App Store Distribution** 描述文件（不仅 Ad Hoc / TestFlight 内测）
- [ ] 第三方 SDK 需 **Privacy Manifest**（`PrivacyInfo.xcprivacy`）时一并提交
- [ ] 新增权限（相机/麦克风/相册等）须在 `Info.plist` 写 **Usage Description**

### 数据与合规（隐私政策正文须覆盖）

- 收集项：Apple ID、用户生成内容、日志等
- 第三方处理：LLM / TTS / 存储服务商及数据保留期
- 用户权利：访问、删除账号
- 面向美国用户时考虑 **CCPA**；13 岁以下则 **COPPA** / 年龄分级

### 首版可延后、但需知悉

- 积分 / IAP：iOS 数字内容须 **StoreKit**，未接入前勿在 App 内卖虚拟商品
- CCPA 数据导出流程、UGC 内容审核策略 — 用户量上来后再补

## 4. 新项目自检（提交 / 上架前）

1. `{slug}` 在 hellotita.top 下 privacy / terms / support 可访问
2. 代码中 API 与法律链接 URL 与上表一致
3. 服务器 `/home/admin/apps/{仓库名称}` 已部署，`backend/deploy/start.sh` 可一键构建并 pm2 启停，进程名 `{slug}-*`
4. iOS：TestFlight 走通登录 → 核心流程 → 删号；Connect 隐私标签与政策 URL 一致
