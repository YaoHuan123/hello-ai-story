# 界面语言（i18n）规划

本文说明 **Web 先行** 下的中英文分工、配置方式，以及 canonical 英文内部存储 + 展示层翻译的架构。**不含原生 App 构建变体**（见文末「原生后续」）。

## 核心架构（当前约定）

```
用户展示语言（displayLocale）     ← API 边界翻译，仅影响 HTTP 响应 / 前端可见文案
        ↑
meta.locale / APP_LOCALE（zh | en）

内部 canonical（始终英文）        ← LLM I/O、落盘、模板 field key、prompt 全文
```

| 层级 | 语言 | 说明 |
|------|------|------|
| **壳层 UI** | `APP_LOCALE` | 按钮、Tab、错误码等：`frontend/src/i18n/locales/{zh,en}.ts` |
| **业务 canonical** | 英文 | 模板 field key、Tier1/2 `pick.name`、sections 节名（catalog）；**采访/选题/出题 LLM 已方案 A** |
| **中文展示** | `meta.locale === zh` | catalog 静态映射；采访链路 **写时中文**；文本/视频/演播室仍多为 **写英文 + 读/TTS 时再译** |

**不存在** `prompts/en/` 双轨；全项目共用一套英文 prompt + 输入 JSON 的 `outputLocale`（采访已接入；文本/视频待接入）。

## 方案 A 范围说明

**方案 A** = `outputLocale` 跟随 `meta.locale`，LLM **写时**输出用户语言；读 API **不再**对同语种落盘内容做 MT；仅 **legacy 纯英文** 旧数据走 `shouldUseLegacyDisplayTranslation`。

### 已覆盖（采访链路）

| 模块 | 路径 | 机制 |
|------|------|------|
| 出题 | `backend/src/question/`（dedupe / colloquialize / suggest / refine / extend） | `withOutputLocale` + `systemWithOutputLocale` |
| 选题 LLM | Tier1～8 `backend/src/topic/recommendTier*.ts` | `localeLlm.ts` |
| 读 API | `toDisplayInterviewQuestionAsync`、`getInterviewChatHistoryForDisplay` | 新数据中文直出；legacy 英文才 `translateTextsForDisplay` |
| 静态展示 | `catalog.zh.json` | 子类名、字段标签、select、hotTopic 域、videoStyles |
| 用户答案 | 聊天历史 | **永不 MT** |

### 故意保留（不必方案 A）

| 项 | 原因 |
|----|------|
| Tier1/2 `pick.name` | catalog **英文 canonical id**；仅 `reason` 等展示字段中文化 |
| `sections[].name` 落盘 | canonical 英文名；展示靠 catalog |
| Legacy 读时 MT | 旧采访英文问句 / 选题 title 兼容 |
| `translate/runtime.ts` 基础设施 | legacy、文章、TTS 跨语种兜底 |

## 当前状态

### 已完成

**P0 壳层**

- 登录、Tab、故事墙、账户、创作入口、常见 API 错误码双语

**P1 子页 UI**

- 访谈 / 文本 / 视频子页按钮、状态、错误提示

**P2 采访链路（方案 A）**

- 新建采访写入 `meta.locale`（默认 `APP_LOCALE`）
- 模板：`template-config.canonical.json` + `backend/config/display/catalog.zh.json`
- `backend/src/topic/catalog.ts` 只读 canonical
- 英文 interview / preprocess / topic prompt；`outputLocale` 注入采访 LLM
- API 展示：`toDisplayInterviewQuestionAsync`、`getInterviewChatHistoryForDisplay`（见上表）
- 选题：`GET .../current` 返回 `options` + `optionReasons`

**P2+ 其它（非方案 A，但已有 locale 触点）**

- `prompts/create-video/` 23 步 prompt 英文化
- `video-styles` 展示：`catalog.zh.json` + `APP_LOCALE`
- 正式文章 **读 API**：`GET .../text/tasks/:id/article` → `translateArticleForDisplay`（英→中 MT + 缓存；文章已是中文则跳过）
- 成片 TTS：按 `meta.locale` 选音色；`voiceoverDisplayTts` 语种与音色不匹配时 zh↔en MT
- `interview-studio/` prompt 英文化；Tier4 `hotTopicDomains` 展示映射

### 待办 backlog（方案 A 推广 + 管线修复）

按推荐推进顺序排列。

#### P2-text — 文本创作（方案 A 已接入生成侧）

| 项 | 状态 |
|----|------|
| 文章 **生成** | `generateArticle.ts` + `step-10_formal-article.md` 注入 `outputLocale`（`runTextPipeline` → `getInterviewDisplayLocale`） |
| 文章 **读 API** | `textArticleDisplay.ts`：已是中文则跳过 MT；legacy 英文仍 `translateArticleForDisplay` |
| sections 入参 | 节名英文 canonical + qa 中文（首期可接受） |

#### P2-video — 传记视频管线（方案 A 已接入生成侧）

| 项 | 状态 |
|----|------|
| `prompts/create-video/` 各 LLM 步 | 统一 `### Output locale` 块（见 `_output-locale.md`）；`buildVideoLlmMessages` + `runWithDisplayLocale` |
| 旁白 TTS | 中文旁白落盘后 `voiceoverDisplayTts` 跳过主路径 MT；跨语种仍兜底 |
| step110/120 | `envSceneExtract.ts` 兼容英文 `timeLabel` / in-at 地点 |
| step250 字幕 | 与旁白同源，随 locale 对齐 |

#### P2-studio — 演播室视频

| 项 | 现状 | 目标 |
|----|------|------|
| `prompts/interview-studio/` | 英文；无 `outputLocale` | 脚本与时长改写按 `meta.locale` 输出 |
| `studioTts.ts` | 按 locale 选音色 | 与脚本文本语种一致 |

#### P3 — 历史与兼容

| 项 | 说明 |
|----|------|
| 旧采访无 `meta.locale` | 视为 `zh` |
| 已 commit 中文答题 | 不追溯翻译 |
| `display-translate-cache.json` | 保留；legacy 文章/问句/TTS 缓存 |

### 已关闭（原「尚未完成」表中划线项）

- `video-styles` 展示层
- 正式文章读 API（有 MT；待 P2-text 生成侧方案 A 后降级为 legacy）
- 成片 TTS 语种校验与 MT 路径
- `interview-studio/` prompt 英文化
- Tier4 `hotTopicDomains` 展示

## 语言如何决定（Web）

| 优先级 | 来源 | 说明 |
|--------|------|------|
| 1 | 后端 `APP_LOCALE` | `GET /api/health` → `locale`；整站壳层权威配置 |
| 2 | `VITE_LOCALE` | health 不可用时的构建兜底 |
| 3 | `navigator.language` | 以上皆无时：`zh*` → 中文，否则英文 |

单条采访的业务**展示**语言以 `meta.locale` 为准（创建时写入，与当时 `APP_LOCALE` 一致）。

## 后台开关

`backend/.env`：

```env
APP_LOCALE=zh   # 或 en
```

修改后**重启后端**。前端 Web 刷新即可。

```bash
curl http://localhost:3001/api/health
# "locale": "zh" | "en"
```

## 模板与 catalog

| 文件 | 角色 |
|------|------|
| `frontend/public/template-config.v2.json` | 中文基线（维护源） |
| `frontend/public/template-config.en.overrides.json` | 构建 canonical 时的英文补丁 |
| `frontend/public/template-config.canonical.json` | **运行时 catalog**（英文节名 + field key） |
| `backend/config/display/catalog.zh.json` | **仅展示**：子类名、字段标签、选项、hotTopic 域、**videoStyles**、基本档案问句 |
| `backend/config/video-styles.en.overrides.json` | 视频风格 canonical 英文（管线 + `APP_LOCALE=en` API） |
| `backend/config/display/field-keys.zh-to-canonical.json` | v2 中文字段 key → canonical 映射 |

改 v2 或 overrides 后执行：

```bash
node backend/scripts/build-canonical-catalog.mjs
```

## 提示词

- 路径：`prompts/{子目录}/{文件名}.md`
- 加载：`backend/src/content/promptPath.ts`、`video/shared/llm/loadPrompt.ts` 等
- **无 locale 分支**；修改 prompt 后需重启 backend（内存缓存）

主要子目录：

| 目录 | 用途 |
|------|------|
| `prompts/interview/` | 出题、选题 tier1–3 gating |
| `prompts/preprocess/` | tier5 矛盾检测等 |
| `topic` 模块内联 prompt 文件 | tier2–8（`topic-pick-tier2.md`、`gap-audit.md` 等，位于 `prompts/interview/`） |
| `prompts/create-text/` | 正式文章 |
| `prompts/create-video/` | 传记视频管线 |
| `prompts/interview-studio/` | 演播室脚本 + 时长对齐改写 |

## 展示翻译实现要点

| 模块 | 路径 |
|------|------|
| 展示语言 | `backend/src/content/displayLocale.ts` |
| 目录映射 | `backend/src/content/displayCatalog.ts` |
| 展示语言 / legacy MT | `backend/src/content/interviewOutputLocale.ts` |
| API 边界 | `backend/src/content/translate/display.ts` |
| MT + 缓存（legacy / 文章 / TTS） | `backend/src/content/translate/runtime.ts` → 采访目录下 `display-translate-cache.json` |
| 正式文章展示 | `backend/src/text/textArticleDisplay.ts` → `translateArticleForDisplay` |
| 聊天历史 | `backend/src/services/interviewChatHistory.service.ts` |
| 选题 reason | `InterviewQuestion.optionReasons`（`interviewOrchestrator.service.ts`） |

`APP_LOCALE=en` 时：壳层与 `meta.locale=en` 的采访**直接展示英文 canonical**，一般不调用 MT。

`meta.locale=zh` 时：结构字段走 catalog；**采访**新数据已是中文则直出；**文本/视频**等仍可能走 MT（见 §待办 backlog）。

## 推进顺序（制片）

1. **P2-text** — 文本创作 `generateArticle` 方案 A（**已完成生成侧**）
2. **P2-video** — 传记视频 LLM + 旁白/TTS/字幕/日期正则（**已完成生成侧**）
3. **P2-studio** — 演播室脚本与 TTS

## 前端文案维护

- `frontend/src/i18n/locales/zh.ts`、`en.ts`
- `t("key")`、`displayError(err)`
- 新增壳层字符串时**中英文 key 同步**

## 相关文件速查

| 区域 | 路径 |
|------|------|
| 后台开关 | `backend/.env` → `APP_LOCALE` |
| Health | `GET /api/health` → `locale` |
| 前端 i18n | `frontend/src/i18n/` |
| 访谈 API | `backend/src/routes/interview.routes.ts` |
| 制片 API | `backend/src/routes/production.routes.ts` |
| 视频风格配置 | `backend/config/video-styles.json` |

## 原生后续（非 Web 范围）

Capacitor 包计划用 `VITE_LOCALE` 构建时写入壳层语言，**不**依赖运行时 `APP_LOCALE`。Web 验收通过后再排期。

## 自检清单（Web）

1. `APP_LOCALE=zh`：访谈冷启动 → 基本档案中文问句 → 选题卡片见中文 `optionReasons` → 聊天历史中文
2. `APP_LOCALE=en`：同上链路英文展示
3. 文本创作（P2-text 后）：`POST /text/tasks` 生成中文正文 → `GET .../article` 不触发英→中 MT
4. 改 `prompts/**/*.md` 后重启 backend，跑一轮选题 / 出题 / 文本 / 视频 prep 确认无混语
